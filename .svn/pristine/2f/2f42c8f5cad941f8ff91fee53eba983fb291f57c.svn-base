using System;
using System.Data;
using System.Text;
using System.Collections.Generic;
using BOSLib;


namespace BOSERP
{
    #region ADLocks
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:ADLocksController
    //Created Date:30 January 2013
    //-----------------------------------------------------------

    public class ADLocksController : BaseBusinessController
    {
        public ADLocksController()
        {
            dal = new DALBaseProvider("ADLocks", typeof(ADLocksInfo));
        }

        /// <summary>
        /// Get locks based on some criteria
        /// </summary>
        /// <param name="fromDate">Date the result is filtered from</param>
        /// <param name="toDate">Date the result is filtered to</param>
        /// <returns>List of locks</returns>
        public List<ADLocksInfo> GetLockList(DateTime fromDate, DateTime toDate)
        {
            DataSet ds = dal.GetDataSet("ADLocks_GetLockList", fromDate, toDate);
            return (List<ADLocksInfo>)GetListFromDataSet(ds);
        }

        public override System.Collections.IList GetListFromDataSet(DataSet ds)
        {
            List<ADLocksInfo> locks = new List<ADLocksInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    ADLocksInfo lockInfo = (ADLocksInfo)GetObjectFromDataRow(row);
                    locks.Add(lockInfo);
                }
            }
            return locks;
        }

        /// <summary>
        /// Get a lock that is in locked state and its locking period
        /// contains a given date
        /// </summary>
        /// <param name="date">Given date</param>
        /// <returns>Lock object</returns>
        public ADLocksInfo GetActiveLockByDate(DateTime date)
        {
            return (ADLocksInfo)dal.GetDataObject("ADLocks_GetActiveLockByDate", date);
        }
    }
    #endregion
}