using System;
using System.Data;
using System.Text;
using System.Collections.Generic;
using BOSLib;


namespace BOSERP
{
    #region MEPatientInss
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:MEPatientInssController
    //Created Date:Thursday, March 24, 2011
    //-----------------------------------------------------------

    public class MEPatientInssController : BaseBusinessController
    {
        public MEPatientInssController()
        {
            dal = new DALBaseProvider("MEPatientInss", typeof(MEPatientInssInfo));
        }

        /// <summary>
        /// Get all patient insuarance
        /// </summary>
        /// <returns>List of all patient insuarance</returns>
        public List<MEPatientInssInfo> GetAllPatientInsuarance()
        {
            List<MEPatientInssInfo> patientInsList = new List<MEPatientInssInfo>();
            MEPatientInssController objPatientInssController = new MEPatientInssController();
            DataSet ds = objPatientInssController.GetAllObjects();
            if (ds.Tables[0].Rows.Count > 0)
            {
                foreach (DataRow dr in ds.Tables[0].Rows)
                {
                    MEPatientInssInfo objPatientInssInfo = new MEPatientInssInfo();
                    objPatientInssInfo = (MEPatientInssInfo)objPatientInssController.GetObjectFromDataRow(dr);
                    patientInsList.Add(objPatientInssInfo);
                }
            }
            return patientInsList;
        }

        public List<MEPatientInssInfo> GetPatientInsListByMEPatientIDAndMECompany(int? patientID, int? companyID)
        {
            DataSet ds = dal.GetDataSet("MEPatientInss_GetPatientInsListByMEPatientIDAndMECompany", patientID, companyID);
            List<MEPatientInssInfo> result = new List<MEPatientInssInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    MEPatientInssInfo objPatientInssInfo = (MEPatientInssInfo)GetObjectFromDataRow(row);
                    result.Add(objPatientInssInfo);
                }
            }
            return result;
        }

        public MEPatientInssInfo GetPatientInsByMEPatientIDAndMECompany(int? patientID, int? companyID)
        {
            DataSet ds = dal.GetDataSet("MEPatientInss_GetPatientInsListByMEPatientIDAndMECompany", patientID, companyID);
            List<MEPatientInssInfo> result = new List<MEPatientInssInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    MEPatientInssInfo objPatientInssInfo = (MEPatientInssInfo)GetObjectFromDataRow(row);
                    result.Add(objPatientInssInfo);
                }
                return result[0];
            }
            else
            {
                return null;
            }
        }
    }
    #endregion
}